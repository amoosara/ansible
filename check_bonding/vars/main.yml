---
  BASE_LOGDIR: "/root/ANSIBLE/playbooks/git/ansible/check_bonding/logs"
  MODULE_NAME: "bonding"
  MODULE_LOG: "{{ BASE_LOGDIR }}/bonding_module_check.out"
  PKG_LOG: "{{ BASE_LOGDIR }}/pkg_check.out"
  PKG_NAME: "ifenslave"
  BOND_NAME_LOG: "{{ BASE_LOGDIR }}/bond_name.log"
